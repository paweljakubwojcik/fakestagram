fragment BasicPostFragment on Post {
    id
    createdAt
    updatedAt
    description
    aspectRatio
    likedByMe
    likeCount
    images {
        id
        url {
            original
            small
        }
    }
    author {
        id
        username
        profileImage
    }
}
