query Profile($username: String!) {
    user(username: $username) {
        id
        username
        posts {
            list {
                id
                images {
                    id
                    url {
                        small
                    }
                }
            }
        }
    }
}
